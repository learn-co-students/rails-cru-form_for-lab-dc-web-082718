<h1><%= @song.name %></h1>

<% if @song.genre != nil %>
  <%= (link_to @song.genre.name, genre_path(@song.genre_id)) %>
<% end %>
<% if @song.artist != nil %>
  <%= (link_to @song.artist.name, artist_path(@song.artist_id)) %>
<% end %>
