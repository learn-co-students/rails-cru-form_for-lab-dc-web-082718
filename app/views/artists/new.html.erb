<%= form_for(@artist) do |f| %>

<label > Artist Name </label> <br>
<%= f.text_field :name %> <br>

<label>Artist Bio</label> <br>
<%= f.text_area :bio %>

<%= f.submit :html=> {:id => "Create Artist"} %>

<%end %>
